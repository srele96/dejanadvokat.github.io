source 'https://rubygems.org'

# Build with the same stack as GitHub Pages for compatibility
gem 'github-pages', group: :jekyll_plugins

# Ruby 3 requires webrick for `jekyll serve`
gem 'webrick', '~> 1.8'

